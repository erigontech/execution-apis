DumpAccounts:
  title: Dumped Accounts
  type: object
  required:
    - root
    - accounts
    - next
  additionalProperties: false
  properties:
    root:
      title: root
      $ref: '#/components/schemas/hash32'
    accounts:
      title: accounts
      $ref: '#/components/schemas/DumpAccount'
    next:
      title: next
      $ref: '#/components/schemas/base64'
DumpAccount:
  title: Dumped Account
  type: object
  required:
    - balance
    - nonce
  additionalProperties: false
  properties:
    address:
      title: balance
      $ref: '#/components/schemas/uint256'
    nonce:
      title: nonce
      $ref: '#/components/schemas/uint64'
    root:
      title: storageHash
      $ref: '#/components/schemas/hash32'
    codeHash:
      title: codeHash
      $ref: '#/components/schemas/hash32'
    code:
      title: code
      $ref: '#/components/schemas/bytes'